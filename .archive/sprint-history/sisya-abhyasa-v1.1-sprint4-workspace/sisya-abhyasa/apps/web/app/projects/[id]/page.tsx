'use client';
import {useEffect,useMemo,useState} from 'react'; import {useParams} from 'next/navigation'; import {api} from '../../../lib/api';
type Task={id:string;title:string;description:string;completion_criteria:string;required_skills:string[];resources:string[];status:'todo'|'in_progress'|'in_review'|'done'};
type Project={id:string;title:string;description:string;difficulty:string;status:string;milestones:{id:string;title:string;objective:string;tasks:Task[]}[]};
const columns:[Task['status'],string][]=[['todo','To Do'],['in_progress','In Progress'],['in_review','In Review'],['done','Done']];
export default function ProjectPage(){const {id}=useParams<{id:string}>();const [p,setP]=useState<Project|null>(null),[error,setError]=useState(''),[busy,setBusy]=useState('');const load=()=>api(`/projects/${id}`).then(setP).catch(e=>setError(e.message));useEffect(()=>{load()},[id]);const tasks=useMemo(()=>p?.milestones.flatMap(m=>m.tasks.map(t=>({...t,milestone:m.title})))??[],[p]);async function move(task:Task,status:Task['status']){setBusy(task.id);try{await api(`/tasks/${task.id}`,{method:'PATCH',body:JSON.stringify({status})});await load()}catch(e:any){setError(e.message)}finally{setBusy('')}}if(error)return <main className="shell formPage"><p className="error">{error}</p></main>;if(!p)return <main className="shell formPage"><p>Loading project…</p></main>;return <main className="shell formPage"><span className="tag">Project workspace · {p.difficulty}</span><h1>{p.title}</h1><p className="lead">{p.description}</p><div className="workspaceNav"><a href="#kanban">Kanban</a><a href="#milestones">Milestones</a><a href="/projects">My Projects</a></div><section id="kanban"><h2>Kanban</h2><p className="note">Status changes are saved to PostgreSQL. Open a task for its learning context.</p><div className="kanban">{columns.map(([key,label])=><div className="kanbanCol" key={key}><h3>{label} <span className="tag">{tasks.filter(t=>t.status===key).length}</span></h3>{tasks.filter(t=>t.status===key).map(t=><article className="task" key={t.id}><a href={`/tasks/${t.id}`}><strong>{t.title}</strong></a><small>{t.milestone}</small><select aria-label={`Status for ${t.title}`} value={t.status} disabled={busy===t.id} onChange={e=>move(t,e.target.value as Task['status'])}>{columns.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></article>)}</div>)}</div></section><section id="milestones" className="planReview"><h2>Milestones</h2>{p.milestones.map((m,i)=><article className="milestone" key={m.id}><span className="tag">Milestone {i+1}</span><h3>{m.title}</h3><p>{m.objective}</p><small>{m.tasks.filter(t=>t.status==='done').length}/{m.tasks.length} tasks done</small></article>)}</section></main>}
