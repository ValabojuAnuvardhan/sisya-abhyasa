'use client';
import {useEffect,useState} from 'react';
import {api} from '../../../lib/api';
type Card={id:string;title:string;pitch:string;difficulty:string;skills_needed:string[];team_size:number;team_capacity:number;slots_available:number;owner_name:string;match_reasons:string[];request_status:string|null};
export default function CommunityDiscover(){const [cards,setCards]=useState<Card[]>([]),[error,setError]=useState(''),[messages,setMessages]=useState<Record<string,string>>({});async function load(){try{setCards(await api('/community/projects'))}catch(e:any){setError(e.message)}}useEffect(()=>{load()},[]);async function join(id:string){try{await api(`/community/projects/${id}/join-requests`,{method:'POST',body:JSON.stringify({message:messages[id]||null})});await load()}catch(e:any){setError(e.message)}}return <main className="shell formPage"><span className="tag">Community Projects</span><h1>Find a team project to join.</h1><p className="lead">Only owner-approved discovery information is shown here. Private tasks, chat, Meet links, GitHub repositories and evidence stay hidden until you are accepted.</p>{error&&<p className="error">{error}</p>}<div className="grid">{cards.map(c=><article className="planReview" key={c.id}><span className="tag">{c.difficulty} · {c.slots_available} slot{c.slots_available===1?'':'s'} open</span><h2>{c.title}</h2><p>{c.pitch}</p><small>Project owner: {c.owner_name} · Team {c.team_size}/{c.team_capacity}</small><h3>Skills needed</h3><div className="referenceRow">{c.skills_needed.length?c.skills_needed.map(s=><span className="refChip" key={s}>{s}</span>):<span className="status">General contributors welcome</span>}</div><h3>Why this may fit you</h3><ul>{c.match_reasons.map(r=><li key={r}>{r}</li>)}</ul>{c.request_status==='pending'?<p className="notice">Join request pending.</p>:c.request_status==='accepted'?<p className="notice">Accepted — this project is now available in My Projects.</p>:c.slots_available>0?<><label>Short note to the owner<textarea value={messages[c.id]||''} onChange={e=>setMessages({...messages,[c.id]:e.target.value})} placeholder="Why are you interested? What can you contribute or learn?"/></label><button className="btn primary" onClick={()=>join(c.id)}>Request to join</button></>:<p className="status">Team currently full.</p>}</article>)}</div>{cards.length===0&&<p className="status">No open community projects yet. Owners can publish collaboration listings from their project workspace.</p>}<a className="btn secondary" href="/dashboard">← Dashboard</a></main>}
